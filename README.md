# Firebase Demo

This is a small demo project showing a basic Firebase setup using a simple HTML page (`index.html`) and JavaScript file (`index.js`).

## How to run

1. Open `index.html` in your browser.
2. Make sure your Firebase configuration in `index.js` is set correctly.
